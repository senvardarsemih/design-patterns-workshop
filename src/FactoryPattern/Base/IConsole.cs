namespace FactoryPattern.Base
{
    // The Console interface declares the operations that all concrete consoles
    // must implement.
    public interface IConsole
    {
        void RunTheGame();
    }
}
